# ⚡ Быстрая настройка нового проекта

## 🎯 Что менять для каждой новой компании?

### 1️⃣ Файл конфигурации (ГЛАВНОЕ!)
📁 **`config/company.config.ts`**

```typescript
name: "Новое название",           // ← ИЗМЕНИТЕ
phone: "+7 (XXX) XXX-XX-XX",      // ← ИЗМЕНИТЕ
email: "new@email.com",            // ← ИЗМЕНИТЕ
address: { ... },                  // ← ИЗМЕНИТЕ
coordinates: { ... },              // ← ИЗМЕНИТЕ (см. инструкцию ниже)
logo: { url: "/new-logo.svg" }    // ← ИЗМЕНИТЕ
```

---

### 2️⃣ Логотип
📁 **`public/transavtopskov-logo.svg`**
- Замените на логотип новой компании
- Формат: SVG (рекомендуется) или PNG
- Размер: ~300x80px

---

### 3️⃣ Координаты на карте
🗺️ **Как получить координаты:**
1. Откройте https://yandex.ru/maps
2. Найдите адрес
3. ПКМ → "Что здесь?"
4. Скопируйте координаты
5. Вставьте в `config/company.config.ts`

---

### 4️⃣ Отзывы (опционально)
📁 **`src/data/reviews.ts`**
- Замените отзывы на реальные
- Или удалите лишние отзывы
- Измените количество в счётчике

---

## 🚀 Команды для запуска

```bash
# 1. Клонировать проект
git clone https://github.com/gorbacenkovitalij6-prog/transavtopskov-yandex-maps.git
cd transavtopskov-yandex-maps

# 2. Установить зависимости
bun install

# 3. Запустить dev сервер
bun dev

# 4. Открыть в браузере
# http://localhost:3000
```

---

## 📝 Чеклист перед запуском

```
[ ] config/company.config.ts - ВСЕ ПОЛЯ ЗАПОЛНЕНЫ
[ ] Логотип заменён
[ ] Координаты проверены на карте
[ ] Номер телефона работает
[ ] Email корректный
[ ] Отзывы обновлены/удалены
[ ] Проект запускается локально
[ ] Готово к деплою
```

---

## ⏱️ Время настройки: 10-15 минут

**Что нужно сделать:**
1. Открыть `config/company.config.ts` → заменить данные (5 мин)
2. Заменить логотип → скопировать в `public/` (2 мин)
3. Получить координаты → Яндекс Карты (3 мин)
4. Запустить и проверить → `bun dev` (2 мин)
5. **ГОТОВО!** ✅

---

## 🎨 Дополнительная настройка (опционально)

### Изменить цвета:
📁 **`tailwind.config.ts`**

### Изменить метаданные:
📁 **`src/app/layout.tsx`**
- title
- description
- favicon

### Изменить фотографии:
📁 **`public/photos/`**
- Замените на фото вашей компании

---

## 🆘 Проблемы?

### Проект не запускается?
```bash
rm -rf node_modules
bun install
bun dev
```

### Карта не отображается?
- Проверьте координаты в `config/company.config.ts`
- Формат: `latitude: 55.751244, longitude: 37.618423`

### Логотип не отображается?
- Проверьте путь в `config/company.config.ts`
- Убедитесь что файл существует в `public/`

---

🎉 **Готово! Создавайте клоны для разных компаний за 10 минут!**
